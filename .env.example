# Optional -- override the default Ollama settings.
# Without changes, the app connects to Ollama at http://localhost:11434
# using the llama3.2 model.

# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2
